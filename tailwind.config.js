module.exports = {
    content: [
        "./src/**/*.html", // All HTML files in the src folder and subfolders
        "./src/**/*.js",   // Include JavaScript files if you're using Tailwind classes in JS
    ],
    theme: {
        extend: {}, // Add custom themes or overrides here
    },
    plugins: [], // Add plugins like forms, typography, etc., if needed
};
